# CTP 论文与 π0.5 接入审计（2026-09-13）

## 结论与证据边界

CTP 的轨迹级有限混合建模与单次前向生成在数学上成立；DAPS 是带有退化通道的重叠正则，不能保证避免模式坍塌；ETBT 是推理时的时序选择器，不能修复训练中的宽度失准或无效候选。当前接入忠实移植了所固定 CTP 源码的 π0.5 动作头和默认损失，但不等于完整复现论文公式、历史最佳配置或 LIBERO 实验。此前验证主要覆盖加载、梯度、恢复和可运行性，对训练配方和统计有效性的验收不充分。

依据：用户提供的 root.pdf 全文 8 页；固定 CTP 源码及历史配置；当前 AutoDL 执行配置与源码；固定验证样本指标和 5k 宽度诊断。没有重新执行论文的仿真实验，论文成绩仅作为作者报告。

## 论文身份与核心方法

- 标题：条件轨迹峰：面向完整动作块的单次前向多模态策略。
- 作者：匿名作者；会议、正式年份、DOI/arXiv/OpenReview：未找到。
- PDF 元数据生成时间：2026-09-12 23:43:38；这不是发表日期。
- PDF SHA256：57108395e70f07d061cea7f4fd33b7cc9f1887613df774cafd61a5052b672831。
- 关联代码参考：Wu-didi/ctp，提交 5bdb96066c219d069c1db16a971eb77cbc407b94；不能由关联关系推断每项论文结果的确切 checkpoint 来源。
- 问题：多解示范中的均值动作、候选坍塌、滚动执行时跨块路线跳变。
- 方法：一个共享表征输出 K 条完整动作块中心、混合概率和一个每轨迹标量 σ；精确边缘混合似然训练；概率加权的尺度相关重叠惩罚；部署时 Top-1 或轨迹对应信念传输。

## 数学审计

### 1. 轨迹级混合成立，但软责任不保证实质上的软学习

第 3 页式 (4)–(7) 定义 d=HD 维各向同性高斯混合。所有时间步共享同一离散变量，这是块内候选身份一致性的合理结构。它不保证候选一定可执行，也不保证候选对应不同语义行为。

对于等先验、等宽度的两个候选，后验 log-odds 差为 d×ΔMSE/(2σ²)。σ=.15、每维 MSE 仅相差 .001 时，较优候选责任为：d=32 时 .670647，d=350 时 .999581，当前 d=700 时 .999999881。数值探针已通过。这说明论文在低维任务中描述的“早期多个候选仍有连续梯度”，在 700 维长动作块中可能几乎退化为 hard winner-takes-all。对最终 NLL 除以 d 不会软化 logsumexp 内部责任。

### 2. DAPS 有明确的概率与宽度逃逸通道

第 3–4 页式 (8)–(9)：π_iπ_j exp[-MSE_ij/(2(σ_i²+σ_j²))] 可以通过三种方式下降：中心分离、某一概率趋零、宽度收缩。后两种不等价于学到额外的有效行为。

对两个近邻候选的独立梯度探针显示，重叠项对已占优 logit 梯度为负、对次要 logit 为正，因此梯度下降会进一步扩大概率差；对两个宽度梯度均为正，因此梯度下降会缩小宽度。这是公式本身的性质，不是浮点错误。论文允许冗余分量退出是合理的，但不能同时把它当作无条件防坍塌保证。

该核采用每维平均距离，并非 d 维高斯分布的精确重叠积分或 Bhattacharyya 系数；尤其宽度不同时缺少尺度比相关因子。称为启发式冗余相似度更准确，不能把核值直接当作有校准保证的统计重叠概率。

### 3. 论文与代码的损失尺度不一致

论文：L_joint + λ_paper L_overlap。

代码：L_joint/d + λ_code L_overlap（省略与参数无关的高斯常数）。

因此 λ_paper = d λ_code 才是相对权重等价关系。当前 d=700、λ_code=.1，相当于论文未归一化形式 λ_paper=70；若目标是论文形式 λ_paper=.1，则代码系数应为 .1/700≈.000142857。PDF 没有给出足以确定各实验实际 λ 的完整配置，不能断言“论文实际就是 .1”。

当前与固定原始 π0.5 CTP 源码在随机 FP64 输入上的 loss 和三组输入梯度最大差均为 0。归一化差异继承自参考代码，并非移植独有错误。将总目标同步缩放保持最优点，但仅缩放 NLL 不保持与正则的相对强度；即使等比例整体缩放，在梯度裁剪、Adam epsilon 等条件下训练动力学也不一定完全相同。

### 4. σ 是受限残差尺度，不是天然校准的不确定度

单模式主导时，每维 loss≈MSE/(2σ²)+logσ。训练误差缩小时，最优 σ 随之缩小；验证误差不再改善时，验证 NLL 会升高。σ 覆盖 50×14 个坐标，无法区分近远期、左右臂及夹爪的异质误差。负 NLL 本身合法；持续升高应结合动作误差和校准量判断。

### 5. ETBT 的合理性与局限

第 4 页式 (11)–(13) 通过上一条实际执行候选的后缀对应当前前缀，避免依赖跨时刻稳定的分量编号，逻辑成立。概率质量更精确地说是当前条件策略输出的权重，不是额外观测模型给出的独立 likelihood；该信念融合应视为有用的递推规则，而非已经证明校准的贝叶斯过滤器。

历史几何约束需要 E<H。它不能恢复训练中已闲置的候选；单模式主导时增益空间有限。当前绝对关节命令不应套用 LIBERO 的前 6 维增量累加；归一化关节空间距离也不能直接声称是机器人末端物理路径距离或 FK 验证。

## 当前实现、历史配置与论文的对应

| 项目 | 当前运行 | 论文/固定参考 | 判断 |
|---|---|---|---|
| 共享 VLM + action expert，零动作/零时间，一次前向，池化后融合线性头 | 已接入 | 固定 π0.5 CTP 代码同样实现 | 不是逐步 MDN；不是原 flow loss |
| K/H/D | 4/50/14 | LIBERO K=4、H=50；动作空间不同 | d 从 LIBERO 常见 7 维控制的 350 增至 700，需重新校准 |
| DCT 基 | 无，直接输出 50×14 | PDF 式 (3) 写 DCT；参考 π0.5 代码也直接输出 | 未实现低秩平滑；满秩情况下表达能力可通过可逆线性变换等价，不是必须重写的 bug |
| 状态输入 | qpos 归一化后离散为 prompt token | π0.5 processor | 状态未丢失；forward 不直接传连续 state 不等于无状态 |
| 绝对/相对动作 | real_action 绝对关节命令；无参考点减法 | PDF 式 (1) 给参考点中心化，允许无参考点时取零 | 当前可训练，但中心化可能降低回归负担；需明确往返变换，不能盲目积分 |
| 主干训练 | 全参数参与模块训练、无 LoRA | PDF LIBERO 冻结视觉语言骨干、训练完整 expert+head | 用户授权的实验扩展，不是论文复现；预训练遗忘风险需实测 |
| anti-collapse 配方 | floor=0、entropy=0、加权 overlap、width 可被 overlap 更新 | historical/source_train_config：floor=.05、entropy=.05、目标 K=2、不加权 overlap、detach_widths=true | 当前使用默认值，不是这份历史配方；不能把历史文件未经来源核对就认作表 V 唯一配方 |
| 训练尾部 padding | CTP loss 没有使用 action_is_pad | 新验证动作指标剔除 padding | 监督语义与评估口径不齐，应显式配置是否用终态重复作为监督 |
| 执行前缀 | n_action_steps=50，Top-1 | 论文 LIBERO 执行 10/预测 50，使用信念选择 | 尚未部署闭环；当前配置无重叠区间，不能声称 ETBT 已启用 |
| 模型选择 | 原内联验证只取首个 val episode 前 128 帧 | 后补全 17 集全量及固定分层 1088 帧 | 旧曲线有采样偏差；新曲线也确认 NLL 上升 |
| 保留 checkpoint | 保留最新 1 个 | 需要按验证动作指标保留 best | 容量策略没有保留质量最优候选，后续应 latest 完整恢复 + best 模型权重 |

相同 learning rate 作用于随机新 head 与预训练主干，且没有单独 head 适应阶段，可能放大早期不稳定；这仍是待对照验证的优化假设，不是已经定位的唯一根因。当前恢复测试、严格基座加载、FSDP 有限梯度验证已通过，不能把 loss 趋势归咎为权重未加载或优化器没有更新。

## 当前曲线直接说明什么

同一 1088 帧、相同动作掩码口径：

| step | masked NLL | Top-1 MSE | 首动作 MSE | 条件有效 K |
|---|---:|---:|---:|---:|
| 1000 | -.9164 | .05161 | .02121 | 1.3133 |
| 2000 | -.7627 | .04080 | .00848 | 1.0172 |
| 3000 | -.3339 | .03823 | .00685 | 1.0029 |
| 4000 | .1170 | .03740 | .00560 | 1.0011 |
| 5000 | 1.2091 | .03825 | .00531 | 1.0006 |

5k 独立 136 帧宽度诊断：选中 σ 均值 .0703，其他头约 .137–.139，逐帧 RMSE 均值 .1662，MSE/σ² 均值 6.676，136 帧都选 mode 0。全部头平均宽度约 .12 掩盖了实际执行头偏窄。全量验证 12883 帧也确认趋势，不是仅旧 128 帧验证的假象。

推断：动作预测早期有明显学习，长动作块误差约 3k–5k 进入平台，而分布越来越自信；近端首动作仍改善。因此不能说所有控制能力都越来越差，也不能只凭 NLL 改善宣称修复。单任务本身可能近似条件单峰；K≈1 是多峰机制退化的诊断，不能单独证明任务失败。

## 实验依据与论文论证缺口

- Push-T：低维 2 帧历史、16×2 动作、K=4；40 epoch、最终 EMA、5 次独立训练、每次 200 个固定初始条件。同骨干单峰 .7731±.0285，CTP .8395±.0174；摘要 .9140 为另外的报告模型得分，不能混作 5 次均值。
- D3IL：状态控制任务，官方 residual MLP；Aligning 6 次训练、60 个共同情境，单峰 249/360、CTP 287/360。DAPS 匹配实验 3 次，报告平均 +4.51 pp；3 个训练种子的 episode-level bootstrap 不应替代训练间不确定性讨论。
- LIBERO：4 套件 40 任务、每任务 10 回合，共 400；CTP 389/400=97.25%，单个选出的 30k checkpoint，seed42；π0.5 本地复测 96.75%，seed1000。两次只差 2 个成功回合且并非严格配对，不能证明 CTP 成功率优于 π0.5。选择最佳 checkpoint 与最终评测集合的独立性没有充分交代。
- LIBERO 延迟：同一 RTX4090、batch1、BF16、未编译、完整调用 100 次，218.24→75.80ms，约 2.88 倍；这是较明确的运行效率证据，但需要原始计时数据/权重验证才能独立确认。
- ETBT：Avoiding 66.67→100%；Aligning 77.78→67.78%，尽管路径切换下降71.55%。后一结果应明确讨论任务性能下降，不能以一致性改善代替成功率改善。
- 表 VI、IX 仍为 TBD，真机图与实验尚为占位。现有证据来自仿真，不能支持“已完成真机验证”或完整结构消融已完成的表述。
- 全球/跨状态头使用熵高，只说明不同状态用了不同编号，不能证明相同条件有多个有用候选；需条件熵、候选覆盖/质量、oracle-vs-top1 和闭环共同验证。
- 可安全引用：作者报告的单次前向结构、指定协议下的结果、有限任务中的多峰收益。不可据此引用：普遍防坍塌保证、普遍优于 π0.5、ETBT 必然提高成功率、168 条真实机器人示范全参数微调已经验证有效。

## 修复顺序与实验边界

1. 保留 6k 完整恢复点后暂停旧训练，保留原配置和指标；检查模型、Adam、scheduler、step、RNG。新分支处理，不热改旧执行 checkout。
2. 先修正训练/验证动作 padding 口径，记录 selected σ、selected MSE/σ²、Top-1、近端前缀及分阶段误差；明确 overlap 系数属于哪种尺度。
3. 保留全参数微调要求，对相同恢复点做短对照：原目标控制组；固定 σ 或宽度正则的校准组；如需检验历史 anti-collapse 配方，单独命名为方法变体。固定宽度改善 NLL 不能作为动作质量改善证据。不得把熵下限、重置多头或强制均匀使用描述为论文原始 DAPS。
4. 学习率缩小必须连同恢复后的 optimizer/scheduler 一起处理，不能只改 JSON。短对照应另设 stop step，保持 20k scheduler horizon，避免 --steps=短步数 自动重缩余弦计划。
5. 采用验证动作误差选择模型，最新完整 checkpoint 支持续训、最佳模型单独保留。17 集已经用于调参，是 validation 而非独立 test；最终还需独立场景或真机闭环。
6. 部署单独比较执行前缀 10 等设置；正确处理绝对关节空间尺度后，再比较 Top-1/ETBT。ETBT 不作为训练 NLL 修复手段。

本审计尚未证明某一修复配方能改善闭环成功率，也未完成修复后的 GPU 对照训练。

## 主实验范围澄清（后续讨论）

主实验仍应保留用户要求的全参数微调：完整 π0.5 FM 与完整 π0.5 CTP，从同一 BASE 出发，使用同一数据及验证/测试协议，给予相当调参预算。冻结 VLM 可作为定位表征更新与动作头优化相互影响的辅助实验，但不能替代全参数主实验，亦不能通过只冻结 FM 来使其处于不利条件。仅冻结设置获胜说明该设置下的优势，不能推出全参数微调或 FM 方法族普遍劣于 CTP。

2026-09-13 查阅的 LeRobot 官方 π0.5 文档同时提供不冻结和冻结 VLM 的训练示例，默认 freeze_vision_encoder=false、train_expert_only=false。因此“没有人冻结”过于绝对，但以完整微调 FM 作为主基线的要求成立。来源：https://huggingface.co/docs/lerobot/pi05 。
